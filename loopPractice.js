//the program is fixed
/*let number = 100;
while (number >= 0) {
  console.log(number);
  number -= 10;
}

//here the user determines the start and end values
let startNum = prompt("Enter the start number");
let endNum = prompt("Enter the end");
while (startNum >= endNum) {
  console.log(startNum);
  startNum -= 10;
}
*/
let numbers = [21, 45, 100, 12, 11, 78, 61, 4, 39, 22];
for (number of numbers) {
  console.log(number);
}
for (number of numbers) {
  if (number % 2 === 0) {
    console.log(number);
  }
}
for (number of numbers) {
  if (number > 10 && number < 60) {
    console.log(number);
  }
}
