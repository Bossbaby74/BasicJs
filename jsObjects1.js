//excercise1 on objects
const ticket = {
  from: "Starting station",
  to: "Ending station",
  price: "$" + 50,
};

console.log(`ticket info: departure: ${ticket.from}`);
console.log(`Destination: ${ticket.to}`);
console.log(`price: ${ticket.price}`);

/*creating an object called ticket that will display the departure point and the destination.*/

// excercise 2 on obj

let person = {};

person.name = "Nzengang";
person.surName = "Gabriel";
console.log(`${person.name}`);
console.log(`${person.surName}`);
//creating a person object that will receive a name and surname and then display them
